<h1>Tracking prospects from the follwing teams</h1>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Team</th>
      <th># of players tracked</th>
    </tr>
  </thead>
  <tbody>
    {{#each model as |team|}}
      <tr>
        <td>{{team.name}} ({{team.code}})</td>
        <td>{{team.playerCount}}</td>
      </tr>
    {{/each}}
  </tbody>
</table>
{{outlet}}
